# 术语表

> 使用范围（2026-09-08）：长期 Agent 学习术语参考。出现于术语表不代表一期需要实现或向普通用户展示；当前一期范围见[范围记录](docs/00-知识库一期范围与待确认事项.md)。

> 随课程持续更新。当前版本：第 1 课后首次整理。
> 每条格式：术语（英文）— 一句话定义。必要时补充"测试关注点"。

## 一、数据接入与知识检索（第 2 课展开）

- **Connector（连接器）** — 持续从 Jira / 飞书 / Notion 等系统同步数据、权限（ACL）和变更事件的组件。测试关注：对账、同步延迟、删除传播、权限映射。
- **Parser（解析器）** — 把 PDF / Word / Excel / 网页等原始文件转成干净文本与结构（表格、标题）的组件。测试关注：表格与扫描件的解析质量。
- **Chunking（切块）** — 把长文档切成适合检索的小段。切不好会把表头和数据、问题和结论切分家。
- **Embedding（向量化）** — 把文本映射为高维向量，语义相近的文本向量距离近。是语义检索的基础。
- **向量数据库（Vector Database）** — 存储向量并支持"找最相似"查询的数据库。
- **BM25** — 经典关键词检索：问句里的字/编号在块里出现得越多、越「稀有」，这块越靠前。对人名、任务编号、工单号比向量可靠。测试时：只开向量、关掉 BM25，专名题应能复现漏召回。
- **Hybrid Search（混合检索）** — 向量检索 + 关键词检索的组合，取长补短，是企业检索的事实标准。
- **Reranker（重排器）** — 对初步检索结果做精细重新排序的模型，提高最终送给 LLM 的上下文质量。
- **Metadata（元数据）** — 每个文本块携带的来源、时间、作者、版本、权限等信息。丢了元数据，检索到内容也无法判断可信度。
- **Permission-aware Retrieval（权限感知检索）** — 检索结果必须按"提问人的权限"过滤。测试关注：跨用户隔离。
- **RAG（Retrieval-Augmented Generation）** — 先检索相关材料、再让 LLM 基于材料回答的架构，用于非结构化知识。测试要拆开：找对没有、用对没有、引用对没有。
- **Top-K** — 检索先取出的候选块数量。K 太小易漏召回，太大则噪声和费用上升。重排后再截成送进模型的 N 段。
- **Groundedness（接地）** — 答案中的断言能否在给定检索块里找到依据。无依据即幻觉。数字/日期应用代码核对。
- **Citation / Attribution（引用）** — 答案标明依据来自哪份文档、哪一段。测的是引用是否真能支撑那句话，不是有没有挂一个文件名。
- **Overlap（切块重叠）** — 相邻块重复一小段，降低「关键句被切断」。测完整句是否至少落在某一块，不测百分比本身。

## 二、结构化数据与指标（第 3 课展开）

- **Database / Data Warehouse / Data Lake** — 业务库（支撑系统运行）/ 数仓（面向分析的整合数据）/ 数据湖（原始数据的廉价存储）。
- **BI / Dashboard（数据看板）** — 把数仓数据可视化成图表。看板是给人看的；Agent 不应"看图"，应查数。
- **Semantic Layer（语义层）** — 把"销售额 = SUM(order.actual_amount)，剔除退款"这类指标口径集中定义的一层，让所有查询共用同一口径。
- **Metric / Dimension（指标 / 维度）** — 指标是被计算的数（销售额），维度是切分角度（按季度、按区域）。
- **数据口径** — 一个指标的精确计算规则（含过滤、时间窗、单位）。口径不一致是企业数据打架的头号原因。
- **Text-to-SQL** — 把自然语言问题翻译成 SQL 的能力。测试关注：生成 SQL 的正确性评测集。
- **Fact Table（事实表，数仓术语）** — Kimball 维度建模里存业务度量的表。与本项目的 Fact Layer 同名不同义，注意区分。

## 三、事实与判断（第 4 课展开）

- **Fact Layer（事实层）** — 本项目核心设计：跨系统、带来源 / 权威度 / 时间 / 权限 / 溯源的结构化事实断言库，是 Agent 判断的"证据库"。非行业现成组件。
- **Raw Fact（原始事实）** — 系统里直接读出的值：截止日期 = 08-30。
- **Derived Fact（推导事实）** — 用确定性规则从原始事实算出的新事实：已逾期 1 天。应由代码计算，不由 LLM 计算。
- **Assessment（评估 / 判断）** — 结合上下文得出的意见："存在延期风险"。是判断不是事实，必须携带证据引用。
- **转述事实（Utterance vs Content）** — "张三说 X"中，"他说过"是事实，"X 是否为真"是待核实的主张。防止错误归因的第一道防线。
- **Provenance（溯源）** — 每个事实可回溯到原始出处（哪个系统、哪条记录、哪个版本、何时抽取）。
- **System of Record（权威源）** — 某类数据的唯一权威系统（销售额以财务系统为准），用于多源冲突时的采信裁决。
- **Freshness（新鲜度）** — 事实距离最后一次验证 / 更新的时间。"完成度 60%（3 天前更新）"和"实时 60%"是两回事。
- **Knowledge Graph（知识图谱）** — 实体（人、项目、文档）+ 关系（负责、依赖、引用）构成的图。Fact Layer 可视为其带时间与溯源的工程化子集。
- **Enterprise Context（企业上下文）** — Agent 判断某个具体情境时，从事实层 + 检索临时组装出的工作上下文。

## 四、主动机制（第 5 课展开）

- **Event-driven Architecture（事件驱动架构）** — 系统由"事件发生"驱动而非"请求到来"驱动。主动式 Agent 的基础形态。
- **Webhook** — 源系统在数据变化时主动推送通知的机制（Jira 工单更新即推送）。
- **定时扫描（Scheduled Scan）** — 周期性巡检（每天 09:00 扫描临期任务），兜住 Webhook 覆盖不到的情况（如"什么都没发生"本身就是异常）。
- **Trigger（触发）** — 事件满足条件后启动 Agent 的机制。测试关注：should / shouldn't trigger 两个方向。
- **Follow-up（跟进）** — 行动后持续追踪：等回复、理解回复、核实说法、扩展调查。需要持久状态机支撑。
- **Escalation（升级）** — 满足规则时把问题连同证据包上报给更高层级。只陈述事实链，不做责任归因。
- **状态机（State Machine）** — 明确定义"当前处于什么状态、什么输入导致什么迁移"的模型。跟进与升级的正确载体。
- **幂等性（Idempotency）** — 同一操作重复执行结果不变。防止 Agent 重复发消息骚扰员工。
- **Human-in-the-loop（HITL，人工在环）** — 高影响动作（涉及绩效、处罚、对外承诺）必须经人工确认才执行。

## 五、评测（第 6 课展开）

- **Golden Dataset（黄金数据集）** — 人工确认过标准答案的评测用例集，是回归测试的基准。
- **Ground Truth（真值）** — 用例的标准答案及其权威依据。谁有资格定义真值，是评测体系的治理问题。
- **Rubric（评分标准）** — 把"我感觉不错"变成可执行的 0/1 判断或 0-4 分档的明确标准。
- **LLM-as-a-Judge** — 用 LLM 按 Rubric 给输出打分。适合语气、相关性等主观维度；数值正确性、权限合规等必须用代码判定。
- **Deterministic Evaluation（确定性评测）** — 用代码直接判定的指标：数值相等、收件人正确、禁止动作未发生。
- **Precision@K / Recall@K** — 检索评测：前 K 条结果里有多少是对的 / 该找到的有多少被找到。
- **MRR / NDCG** — 检索排序质量指标：正确结果排得越靠前分越高。
- **Event Recall / Event Precision** — 该产生的事件产生了多少 / 产生的事件里多少是真的。
- **Trigger Precision / Trigger Recall** — 触发的干预里多少是应该的 / 应该干预的里多少被触发了。
- **False Intervention Rate（误干预率）** — 不该打扰却打扰的比例。主动式 Agent 的头号信任杀手指标。
- **Inter-Annotator Agreement（IAA，标注者间一致性）** — 多人标注的一致程度（如 Cohen's Kappa）。一致性低说明 Rubric 定义不清。
- **Online Evaluation（在线评测）** — 上线后用真实行为验证：回复率、采纳率、被忽略率、人工纠正率。

## 六、治理与安全（贯穿全程）

- **ACL（Access Control List，访问控制列表）** — 谁能访问哪条数据的清单，需要从源系统同步并在检索、生成、发送三处执行。
- **Prompt Injection（提示注入）** — 攻击者把恶意指令藏进 Agent 会读取的内容（文档、员工回复）中，诱导 Agent 执行。主动式 Agent 的核心攻击面。
- **Cross-user Isolation（跨用户隔离）** — A 的私有数据绝不出现在给 B 的输出里。
- **Audit Log（审计日志）** — Agent 每次感知、判断、行动的完整留痕，事后可回放解释。
- **Human Override（人工接管）** — 人工可随时叫停、纠正 Agent 的机制。
- **Rollback（回滚）** — 事实错误或动作错误后的撤销路径。
